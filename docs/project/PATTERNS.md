# Patterns

This file contains user-accepted implementation patterns that should make future similar choices Warm or Cold.

Propose a pattern in an issue plan or review. Add it here only after the user accepts the proposal, use the next sequential `PAT` ID, and cite the issue where it was accepted.

## Entry Format

```text
## PAT-001 - <Name>

- Use when:
- Pattern:
- Why:
- Origin: `GH-XXX`
```

No project-specific implementation patterns have been accepted yet.
